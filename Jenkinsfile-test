#!groovy
@Library('waluigi@feature/DEVOPS-824') _

timestamps {
  tinyPods.nodeBrowser {
    stage('install') {
      yarnInstall()
    }

    stage('test') {
      sh 'yarn test-selenium'
    }
  }
}